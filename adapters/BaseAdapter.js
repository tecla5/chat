export default class BaseAdapter {
  constructor(options) {        
  }
  
  closeConnection() {    
  }
  
  // onSuccess: function
  syncState(options) {  
    // options.onSuccess();
  }
  
  post(conversationId, message, options) {
    // options.onSuccess();
  }
}  
